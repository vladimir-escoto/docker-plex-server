# =======================
# Variables generales
# =======================

# Zona horaria para todos los contenedores (https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TIMEZONE=Europe/Madrid

# Ruta base donde se almacenan las configuraciones persistentes de los servicios
CONFIG_PATH=/data/config

# Ruta base para la biblioteca multimedia (subcarpetas movies, tv, etc.)
MEDIA_PATH=/data/media

# Identificadores de usuario y grupo del sistema host que poseerán los archivos creados por los contenedores
PUID=1000
PGID=1000

# =======================
# Servicios básicos
# =======================

# Token opcional para reclamar el servidor Plex (https://plex.tv/claim). Puedes dejarlo vacío después del reclamo inicial
PLEX_CLAIM_TOKEN=

# Contraseña del usuario principal de Postgres. Genera un valor seguro con: openssl rand -hex 32
POSTGRES_PASSWORD=postgres_password

# =======================
# Configuración de VPN (Gluetun)
# =======================

# Proveedor del servicio VPN (por ejemplo: mullvad, protonvpn, pia)
VPN_SERVICE_PROVIDER=

# Tipo de túnel soportado por el proveedor (por ejemplo: openvpn, wireguard)
VPN_TYPE=openvpn

# Región o servidor a utilizar según el proveedor (por ejemplo: Spain, Netherlands)
SERVER_REGIONS=
